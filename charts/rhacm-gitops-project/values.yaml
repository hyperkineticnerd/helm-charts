# Default values for rhacm-gitops-project.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.


clusterName: 'prod'
clusterApiServer: 'https://api.prod.openshift.hkn.lab:6443'
# managedNamespaces: ''

sourceRepos:
  - '*'

clusterResourceWhitelist:
  - group: '*'
    kind: '*'
