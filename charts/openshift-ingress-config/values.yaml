# Default values for openshift-ingress-config.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

ingressController:
  existingSecret: my-ingress-certs

clusterProxy:
  existingSecret: ''
