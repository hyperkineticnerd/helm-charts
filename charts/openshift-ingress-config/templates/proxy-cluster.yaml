apiVersion: config.openshift.io/v1
kind: Proxy
metadata:
  name: cluster
  annotations:
    argocd.argoproj.io/sync-options: Delete=false,ServerSideApply=true
spec:
  trustedCA:
    name: {{ .Values.clusterProxy.existingSecret | default "" | quote }}
