apiVersion: cluster.open-cluster-management.io/v1beta1
kind: Placement
metadata:
  name: gitops-clusters
  namepsace: openshift-gitops
spec:
  tolerations:
    - key: cluster.open-cluster-management.io/unrerachable
      operator: Exists
    - key: cluster.open-cluster-management.io/unavailable
      operator: Exists
  # predicates:
  #   - requiredClusterSelector:
  #       labelSelector:
  #         matchExpressions:
  #           - key: vendor
  #             operator: "In"
  #             values:
  #               - OpenShift
  #           - key: local-cluster
  #             operator: DoesNotExist
  # clusterSets:
  #   - test
